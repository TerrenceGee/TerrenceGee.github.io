# 领域驱动设计

## 领域间交互的细节

需求1：domainB需要调用domainA，但是domainB现在只有domainB的entity，如何在domainA设计API；\
正确的做法是引入防腐层，在防腐层将domainBEntity转换为domainAInput实体，再进行API调用。；\
原则上一个领域不知道另一个领域的存在，所以domainA只能接受自己的input，而非其他domain的实体。

需求2：domainA的返回结果是domainA的实体，应该转换为调用方的实体吗？\
当然不是，正确的做法也应该是domainB的防腐层进行转换。
